{
  "name": "project",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "docker-compose up",
    "build": "docker-compose build",
    "build:dev": "docker-compose build && docker-compose up",
    "prod": "docker-compose start"
  }
}
